# Hardcore Player - Environment Configuration

# ============ Required ============

# HuggingFace token (for pyannote.audio speaker diarization)
# Get from: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# OpenAI API (for translation)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# ============ Model Settings ============

# Whisper model: tiny, base, small, medium, large-v2, large-v3
WHISPER_MODEL=large-v3
WHISPER_DEVICE=cuda

# Diarization device
DIARIZATION_DEVICE=cuda

# TTS device (optional, only if using dubbing mode)
TTS_DEVICE=cuda

# ============ Server Settings ============

HOST=0.0.0.0
PORT=8000
DEBUG=false

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=["http://localhost:3000"]

# ============ Video Settings ============

# Max video duration in seconds (2 hours = 7200)
MAX_VIDEO_DURATION=7200

# Use NVIDIA GPU encoding (requires NVENC)
FFMPEG_NVENC=true

# ============ YouTube Upload (Optional) ============

# Download OAuth credentials from Google Cloud Console
# 1. Go to https://console.cloud.google.com
# 2. Create project > Enable YouTube Data API v3
# 3. Create OAuth 2.0 credentials (Desktop app)
# 4. Download JSON and save to credentials/youtube_oauth.json

YOUTUBE_CREDENTIALS_FILE=credentials/youtube_oauth.json
YOUTUBE_TOKEN_FILE=credentials/youtube_token.json

# ============ n8n Automation (Optional) ============

N8N_USER=admin
N8N_PASSWORD=hardcoreplayer
